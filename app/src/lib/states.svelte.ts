export const ajastinPaalla = $state({
	on: false
});

export const sDeath = $state({
	on: false
});

export const harkka = $state({
	on: false
});

export const musiikkiMuted = $state({
	on: true
});
